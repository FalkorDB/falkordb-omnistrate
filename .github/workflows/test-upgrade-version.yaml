name: Test Upgrade Version

permissions:
  contents: read

on:
  workflow_dispatch:
    inputs:
      service_id:
        description: "Omnistrate service ID (overrides OMNISTRATE_INTERNAL_SERVICE_ID var)"
        required: false
        type: string
      environment_id:
        description: "Omnistrate environment ID (overrides OMNISTRATE_INTERNAL_PROD_ENVIRONMENT var)"
        required: false
        type: string
      tls:
        description: "Override TLS for all tiers (use-default = per-tier defaults)"
        required: false
        type: choice
        options:
          - use-default
          - "true"
          - "false"
        default: use-default
      dry_run:
        description: "Simulate only — print planned upgrades without running tests"
        required: false
        type: boolean
      tier_free:
        description: "Test FalkorDB Free tier (default: true)"
        required: false
        type: boolean
        default: true
      tier_startup:
        description: "Test FalkorDB Startup tier (default: true)"
        required: false
        type: boolean
        default: true
      tier_pro:
        description: "Test FalkorDB Pro tier (default: true)"
        required: false
        type: boolean
        default: true
      tier_enterprise:
        description: "Test FalkorDB Enterprise tier (default: true)"
        required: false
        type: boolean
        default: true

jobs:
  generate-matrix:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install requests
        run: pip install requests --quiet

      - name: Fetch upgrade matrix from Omnistrate API
        id: set-matrix
        env:
          OMNISTRATE_USERNAME:          ${{ secrets.OMNISTRATE_USERNAME }}
          OMNISTRATE_PASSWORD:          ${{ secrets.OMNISTRATE_PASSWORD }}
          OMNISTRATE_SERVICE_ID:        ${{ inputs.service_id || vars.OMNISTRATE_INTERNAL_SERVICE_ID }}
          OMNISTRATE_ENV_ID:            ${{ inputs.environment_id || vars.OMNISTRATE_INTERNAL_PROD_ENVIRONMENT }}
          TLS:                          ${{ inputs.tls }}
          DRY_RUN:                      ${{ inputs.dry_run }}
          TIER_FREE:                    ${{ inputs.tier_free }}
          TIER_STARTUP:                 ${{ inputs.tier_startup }}
          TIER_PRO:                     ${{ inputs.tier_pro }}
          TIER_ENTERPRISE:              ${{ inputs.tier_enterprise }}
          GITHUB_RUN_ID:                ${{ github.run_id }}
        run: python3 scripts/fetch_upgrade_matrix.py


  test-upgrade:
    name: ${{ matrix.name }}
    needs: generate-matrix
    if: ${{ !inputs.dry_run && needs.generate-matrix.outputs.matrix != '' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.generate-matrix.outputs.matrix) }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Cache Poetry virtualenv
        id: cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/pypoetry/virtualenvs
          key: ${{ runner.os }}-poetry-${{ hashFiles('poetry.lock') }}
          restore-keys: |
            ${{ runner.os }}-poetry-

      - name: Install Poetry and dependencies
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          export PATH="$HOME/.local/bin:$PATH"
          poetry install

      - name: Run upgrade test
        env:
          OLD_VERSION:     ${{ matrix.old_version }}
          NEW_VERSION:     ${{ matrix.new_version }}
          REF_NAME:        ${{ matrix.ref_name }}
          SUBSCRIPTION_ID: sub-GJPV3NoNC0
        run: |
          export PATH="$HOME/.local/bin:$PATH"

          SERVICE_ID="${{ inputs.service_id || vars.OMNISTRATE_INTERNAL_SERVICE_ID }}"
          ENVIRONMENT_ID="${{ inputs.environment_id || vars.OMNISTRATE_INTERNAL_PROD_ENVIRONMENT }}"

          EXTRA_ARGS=()
          [[ -n "${{ matrix.host_count }}" ]]       && EXTRA_ARGS+=(--host-count       "${{ matrix.host_count }}")
          [[ -n "${{ matrix.cluster_replicas }}" ]] && EXTRA_ARGS+=(--cluster-replicas "${{ matrix.cluster_replicas }}")
          [[ -n "${{ matrix.custom_network }}" ]]   && EXTRA_ARGS+=(--custom-network   "${{ matrix.custom_network }}")
          [[ "${{ matrix.tls }}" == "true" ]]       && EXTRA_ARGS+=(--tls)

          poetry run python -u tests/test_upgrade_version.py \
            "${{ secrets.OMNISTRATE_USERNAME }}" \
            "${{ secrets.OMNISTRATE_PASSWORD }}" \
            aws us-east-2 \
            --service-id     "$SERVICE_ID" \
            --environment-id "$ENVIRONMENT_ID" \
            --resource-key   "${{ matrix.resource_key }}" \
            --instance-name  "${{ matrix.instance_name }}" \
            --instance-type  "${{ matrix.instance_type }}" \
            "${EXTRA_ARGS[@]}"
